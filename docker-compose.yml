version: "3"

services:
  databse:
    platform: linux/x86_64
    image: mysql
    restart: always
    environment:
      - MYSQL_DATABASE=mysql
      - MYSQL_USER=user
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_PASSWORD=password
    ports:
      - "3307:3306"
